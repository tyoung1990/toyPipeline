This is a vignette!
Assign Nove to the task of writing this vignette.
Need to start by finding an example dataset.
I probably should not be adding this in right now!
this is a qmd, so we need a header and we can include output in the vignette.

